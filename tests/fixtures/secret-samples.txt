# True positive samples (patterns that should trigger detection)
# NOTE: Do not put real secrets here. These are known test patterns.
#
# AWS Access Key ID pattern: "AKIA" + 16 uppercase alphanumeric chars.
# The canonical AWS docs example key is intentionally NOT stored verbatim
# here — secret scanners flag it even with suppression markers. Tests
# assemble it at runtime from parts instead (tests/hooks/test-secret-patterns.sh):
#   prefix "AKIA" + body "IOSFODNN7EXAMPLE"
